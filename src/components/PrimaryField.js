/*
import {Field} from "formik";
import '../assets/css/components/Fields.css';


export const PrimaryField = (
    {
        text,
        type,
        placeholder,
        className,
        errors,
        touched,
        required,
        onChange,
        onBlur,
        value,
        disabled,
        ...props
    }) => {
    return (
        <>
            <Field
                name={`${text}`} className={`${className} login-field`}
                placeholder={`${placeholder}`}
            />
            {errors.email && touched.email ? (
                <div className='alert alert-danger'>
                    {errors.email}
                </div>
            ) : null}
        </>

    );

}*/
